# Проект для сдачи домашнего задания по теме "Авторизация в API и работа с API-ключами"

## Описание проекта
Учебный проект для освоения принципов безопасной работы с REST API, включая получение, хранение и использование API-ключей. Реализация демонстрирует корректное взаимодействие с защищенными API, обработку ошибок авторизации и создание пользовательского интерфейса для работы с погодным API.

## Реализованные требования

### ✅ Обязательные задания (минимум):
1. **Создание `.env` и `.gitignore`** - безопасное хранение API-ключа в переменных окружения
2. **Установка зависимостей** - `requests` и `python-dotenv` в файле `requirements.txt`
3. **Реализация модуля `weather_app.py`**:
   - Загрузка ключа через `load_dotenv()` и `os.getenv("OPENWEATHER_API_KEY")`
   - Функция `get_coordinates(city:str) -> tuple[float,float]` с использованием OpenWeather Geocoding API (limit=1, lang=ru)
   - Функция `get_weather_by_coordinates(lat:float, lon:float) -> dict` с использованием Current Weather API (units=metric&lang=ru)
   - Обработка ошибок: статус ≠ 200, пустой ответ геокодинга, невалидный ключ с понятными сообщениями (без трейсбеков)
4. **CLI интерфейс** - запрос города у пользователя с выводом строки: "Погода в <город>: <температура>°C, <описание>"

### ✅ Дополнительные задания (средний уровень):
1. **Режимы ввода** - меню с выбором: 1 — по городу, 2 — по координатам, 0 — выход
2. **Кэширование** - сохранение последнего успешного ответа в `weather_cache.json` с метаданными (city, lat, lon, fetched_at). При сетевой ошибке — предложение вывести данные из кэша, если им < 3 часов
3. **Троттлинг/ретраи** - при 429 (Too Many Requests) или временных сетевых ошибках — до 3 повторов с экспоненциальной паузой (1s, 2s, 4s)

## Техническая реализация

### Структура проекта:
```
weather_app_project/
├── weather_app.py          # Основной модуль со всей логикой приложения
├── requirements.txt        # Зависимости: requests, python-dotenv
├── .env                    # Файл с переменными окружения (API-ключ)
├── .gitignore             # Игнорируемые файлы: .env, venv/, weather_cache.json
├── README.md              # Документация (этот файл)
└── (автоматически создаются)
    ├── venv/              # Виртуальное окружение
    └── weather_cache.json # Файл кэша погодных данных
```

### Ключевые функции:
1. **Безопасная работа с API-ключами** - ключ хранится только в `.env` файле, который исключен из Git
2. **Геокодинг городов** - автоматическое преобразование названий городов в координаты
3. **Получение погодных данных** - запрос актуальной погоды по координатам
4. **Обработка ошибок авторизации** - специальные исключения для невалидных ключей и недоступных городов
5. **Механизм повторных попыток** - интеллектуальная система ретраев при временных проблемах

### Используемое API:
- **OpenWeatherMap API** (`https://openweathermap.org/api`) - основной источник погодных данных:
  - Geocoding API - для преобразования названий городов в координаты
  - Current Weather Data API - для получения актуальной погоды
- **Особенности работы**: API требует авторизации по ключу, предоставляет данные на русском языке в метрической системе

## Инструкция по запуску

### 1. Получение API-ключа:
1. Зарегистрируйтесь на [OpenWeatherMap](https://openweathermap.org/api)
2. В личном кабинете создайте API-ключ
3. Скопируйте полученный ключ

### 2. Настройка проекта:
```bash
# Клонируйте/распакуйте проект
cd weather_app_project

# Создайте виртуальное окружение
python -m venv venv

# Активируйте окружение
# Windows:
.\venv\Scripts\activate
# macOS/Linux:
# source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env и добавьте API-ключ
# Файл .env должен содержать:
# OPENWEATHER_API_KEY=ваш_ключ_от_openweather
```

### 3. Запуск программы:
```bash
python weather_app.py
```

### 4. Примеры использования:
```
==================================================
🌤️  ПРОГНОЗ ПОГОДЫ (OpenWeatherMap)
==================================================

Режимы ввода:
  1 - По названию города
  2 - По координатам (широта, долгота)
  0 - Выход

Выберите режим (0-2): 1
Введите название города: Москва

📍 Получаем погоду для города: Москва

☁️  Погода в Москва: 12.4°C, пасмурно
   💧 Влажность: 78% | 💨 Ветер: 3.2 м/с
```

## Особенности реализации

1. **Безопасность** - API-ключ никогда не хранится в коде, используется файл `.env`, который исключен из Git
2. **Устойчивость к ошибкам** - комплексная обработка сетевых проблем, ошибок API и пользовательского ввода
3. **Производительность** - кэширование результатов и система повторных попыток для работы при нестабильном соединении
4. **Пользовательский опыт** - русскоязычный интерфейс, форматированный вывод с эмодзи, подсказки при ошибках
5. **Модульность** - весь функционал реализован в одном файле для простоты развертывания

## Критерии приемки, которые выполняет проект:
- ✅ Код запускается в чистом окружении (venv, requests, python-dotenv)
- ✅ API-ключ безопасно хранится в `.env` и не попадает в Git
- ✅ Ошибки (невалидный ключ, город не найден, проблемы сети) не вызывают падение программы
- ✅ При сетевых ошибках предлагается использовать кэшированные данные
- ✅ Реализованы все три режима работы (город, координаты, кэш)

## Формат сдачи
- **GitHub-репозиторий** с полным кодом проекта
- **README.md** с описанием и инструкциями (этот файл)
- **Скриншоты** работы программы (по желанию)

Проект готов к проверке. Все требования задания выполнены.
