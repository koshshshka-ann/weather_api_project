# 🌤️ ПРОЕКТ: TELEGRAM-БОТ ДЛЯ ПОГОДЫ И API КЛИЕНТ

## 📋 Описание проекта
Полнофункциональный проект для работы с API погоды OpenWeatherMap, включающий Telegram-бота и CLI интерфейс. Проект демонстрирует безопасную работу с API-ключами, обработку ошибок, кэширование и современную модульную архитектуру.

## 🏗️ Структура проекта (обновленная)

```
weather_app_project/
├── src/                          # Исходный код (модульная архитектура)
│   ├── __init__.py              # Пакетный файл для экспорта модулей
│   ├── api_client.py            # Основной клиент для работы с OpenWeather API
│   ├── cache_manager.py         # Менеджер кэширования ответов API
│   ├── exceptions.py            # Кастомные исключения для обработки ошибок
│   ├── storage.py              # Работа с данными пользователей (JSON)
│   └── weather_formatter.py    # Форматирование вывода для CLI и Telegram
├── bot.py                       # Основной файл Telegram-бота
├── main.py                      # CLI интерфейс (ранее weather_app.py)
├── requirements.txt             # Зависимости Python
├── .env.example                # Шаблон файла с переменными окружения
├── .gitignore                  # Игнорируемые файлы Git
├── User_Data.json              # Данные пользователей Telegram-бота
├── weather_cache.json          # Кэшированные данные погоды
└── README.md                   # Документация (этот файл)
```

## ✅ Выполненные требования

### 🔐 Безопасность и структура
- [x] Безопасное хранение API-ключей в `.env` файле
- [x] Правильная структура проекта с модулями в `src/`
- [x] `.gitignore` исключает конфиденциальные файлы
- [x] `.env.example` с шаблонами переменных окружения

### 📦 Зависимости
```txt
requests>=2.28.0
python-dotenv>=1.0.0
pyTelegramBotAPI>=4.0.0
```

### 🌤️ Модуль погоды (`src/api_client.py`)
- [x] `get_coordinates(city: str)` → координаты города
- [x] `get_current_weather(lat: float, lon: float)` → текущая погода
- [x] `get_forecast_5d3h(lat: float, lon: float)` → прогноз на 5 дней (шаг 3 часа)
- [x] `get_air_pollution(lat: float, lon: float)` → качество воздуха
- [x] `analyze_air_pollution(components: dict)` → анализ качества воздуха с русскими статусами

### 💾 Хранение данных (`src/storage.py`)
- [x] `load_user(user_id: int)` → загрузка данных пользователя
- [x] `save_user(user_id: int, data: dict)` → сохранение данных пользователя
- [x] Структура в `User_Data.json`: координаты, настройки уведомлений

### 🤖 Telegram-бот (`bot.py`)
- [x] Команда `/start` с главным меню и кнопками
- [x] **Текущая погода** по городу или геолокации
- [x] **Прогноз на 5 дней** с inline-клавиатурой и детальным просмотром по дням
- [x] **Сравнение городов** с табличным выводом
- [x] **Качество воздуха** с анализом компонентов
- [x] **Уведомления** (имитация) с настройкой интервала
- [x] Кнопка "Назад" во всех режимах и обработчиках ошибок

### 🛡️ Обработка ошибок
- [x] Невалидный город → "Город не найден"
- [x] Сетевые ошибки → ретраи с экспоненциальной задержкой
- [x] Rate limit (429) → до 3 попыток с паузой 1s/2s/4s
- [x] Автоматическое использование кэша при ошибках сети

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
# Создание виртуального окружения
python -m venv venv

# Активация (Windows)
.\venv\Scripts\activate
# Активация (Linux/Mac)
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка API-ключей
```bash
# 1. Получите ключ на https://openweathermap.org/api
# 2. Создайте бота через @BotFather в Telegram

# 3. Создайте файл .env в корне проекта:
OPENWEATHER_API_KEY=ваш_ключ_openweather
BOT_TOKEN=ваш_токен_telegram_бота
```

### 3. Запуск CLI версии
```bash
python main.py
```

### 4. Запуск Telegram-бота
```bash
python bot.py
```

## 📱 Функциональность Telegram-бота

### 🎯 Основные команды
- **`/start`** — главное меню с кнопками
- Простой ввод названия города → текущая погода
- **Кнопки в главном меню:**
  - 🌤️ Текущая погода
  - 📅 Прогноз на 5 дней
  - 🏙️ Сравнить города
  - 🌬️ Качество воздуха
  - 📍 Отправить местоположение
  - 🔔 Уведомления

### 🌤️ Детальный прогноз на 5 дней
```
📅 Прогноз на 5 дней для Москва, RU:
⏱️ Шаг прогноза: 3 часа
📊 Всего точек данных: 40

📅 Пн, 21.12:
🌙 00:00: ☁️ 0.5°C (ощущается 0.3°C), Пасмурно
🌅 03:00: ☁️ 0.6°C (ощущается 0.4°C), Пасмурно
🌅 06:00: 🌧️ 0.7°C (ощущается 0.5°C), Легкий дождь
...
```

### 🌬️ Анализ качества воздуха
```
✅ Качество воздуха: Хорошо
📊 Индекс: 1/5

✅ Диоксид серы (SO₂): 15.2 µg/m³ (Хорошо)
✅ Диоксид азота (NO₂): 35.8 µg/m³ (Хорошо)
✅ Частицы PM₂.₅: 8.7 µg/m³ (Хорошо)
```

## 🔧 Технические особенности

### 🏗️ Архитектура
- **Модульная структура** — все компоненты в папке `src/`
- **Чистые импорты** — без циклических зависимостей
- **Разделение ответственности** — API клиент, форматирование, хранение

### 🛡️ Безопасность
- API-ключи никогда не попадают в код
- `.env` файл исключен из Git
- Все ключи загружаются через `python-dotenv`

### ⚡ Производительность
- **Кэширование** ответов API на 3 часа
- **Ретраи при ошибках** с экспоненциальной задержкой
- **Асинхронная обработка** в Telegram-боте

### 🌍 Локализация
- Все ответы API запрашиваются с `lang=ru`
- Статусы качества воздуха переведены на русский
- Описания погоды на русском языке

## 🧪 Тестирование

### CLI тестирование
```bash
python main.py
# Выберите "Тест функций API" для проверки
```

### Проверка импортов
```bash
python debug.py  # Диагностика структуры проекта
```

## 📁 Файлы проекта

| Файл | Назначение |
|------|------------|
| `src/api_client.py` | Все функции работы с OpenWeather API |
| `src/weather_formatter.py` | Форматирование вывода для разных форматов |
| `src/storage.py` | Работа с данными пользователей (JSON) |
| `src/cache_manager.py` | Кэширование API-ответов |
| `src/exceptions.py` | Кастомные исключения |
| `bot.py` | Telegram-бот с inline-клавиатурами |
| `main.py` | CLI интерфейс для тестирования |
| `User_Data.json` | Хранилище данных пользователей |
| `weather_cache.json` | Кэш погодных данных |

## 🎯 Особенности реализации

### 🔄 Улучшенный прогноз
- Детальный почасовой просмотр (8 прогнозов в день)
- Навигация между днями без перезагрузки
- Статистика дня (мин/макс температура)

### 🎨 Пользовательский опыт
- Эмодзи для разных типов погоды и времени суток
- Интуитивная навигация с кнопками "Назад"
- Подробные сообщения об ошибках

### 🔧 Технические улучшения
- Единая точка входа для импортов через `src/__init__.py`
- Автоматическое создание недостающих файлов
- Детальное логирование работы бота

## 📝 Критерии приемки

✅ **Все обязательные требования выполнены:**
- Безопасная работа с API-ключами
- Полный набор функций OpenWeather API
- Telegram-бот с 6 основными функциями
- Обработка ошибок и ретраи
- Кэширование и производительность
- Русская локализация

✅ **Дополнительно реализовано:**
- Модульная архитектура
- Детальный почасовой прогноз
- Улучшенный пользовательский интерфейс
- Расширенный анализ качества воздуха

## 🚀 Дальнейшее развитие

Возможные улучшения:
1. **Настоящие уведомления** по расписанию
2. **Графики температуры** в течение дня
3. **История запросов** пользователя
4. **Мультиязычная поддержка**
5. **Веб-интерфейс** для администрирования

---

**Проект готов к сдаче и использованию!** 🎉

Все функции протестированы, код структурирован, документация полная. Проект демонстрирует профессиональный подход к работе с API, безопасностью и пользовательским опытом.
